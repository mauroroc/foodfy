{% extends "admin/adm_layout.njk" %}

{% block content %}

    <div class="detail-top">
        <div class="detail-top-title">
            <h3>Usuários</h3>
        </div>
        <div class="detail-top-button">
            <a href="/admin/users/create">Novo</a>
        </div>
    </div>

    <section class="adm_users">
    {% for item in items %}
        <div class="adm_user_item">
            <div>
                <h4>
                    {{item.name}}
                </h4>
                <h3>
                    {{item.email}}
                </h3>
                <a href="/admin/users/{{item.id}}/edit"><strong>Editar ou Remover</strong></a>
            </div>
        </div>
    {% endfor %}
    </section>
{% endblock %}