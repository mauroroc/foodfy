{% extends "admin/adm_layout.njk" %}

{% block content %}
        <script src="/js/upload.js"></script>
        <div class="detail-top">
            <div class="detail-top-title">
                <h3>Editando Chef</h3>
            </div>
            <div class="detail-top-button">
                <form method="POST" id="form-delete" action="/admin/chefs?_method=DELETE">
                    <input type="hidden" name="idDel" value="{{item.id}}">
                    <button type="submit" class="btnDelete">Deletar</a>                    
                </form>
                <form method="POST" action="/admin/chefs?_method=PUT" enctype="multipart/form-data">
                <button class="btnSave">Salvar Chef</a>
            </div>
        </div>
        <section class="form-receipt">
            {% include "admin/chefs_fields.njk" %} 
            </form>
        </section>   

        <script>
        const formDelete = document.querySelector("#form-delete");
        formDelete.addEventListener("submit", (event) => {
            const confirmation = confirm("Tem certeza que deseja excluir esse Chef?");
            if(!confirmation) {
                event.preventDefault();
            }
        })
    </script>
    
{% endblock %}