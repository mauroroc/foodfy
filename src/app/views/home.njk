{% extends "layout.njk" %}

{% block content %}

    <div id="banner">
        <div class="banner_texto">
            <h1>As melhores Receitas</h2>
            <p>Aprenda a construir os melhores pratos com receitas criadas por profissionais do mundo inteiro.</p>
        </div>
        <div class="banner_imagem">
            <img src="img/chef.png" alt="Imagem do Mascote do Foodfy">
        </div>
    </div>

    <h2 class="cards-title">Últimas Cadastradas</h2>

    <section class="cards">
    {% for item in items %}
        <div class="card_item">
            <a href="/receita/{{item.id}}">
                <img src="{{item.src}}" alt="{{item.title}}">
                <h4>
                    {{item.title}}
                </h4>
                <p>
                    {{item.namechef}}
                </p>
            </a>
        </div>
    {% endfor %}
    </section>

{% endblock %}